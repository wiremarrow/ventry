You are to methodically walk through the CLAUDE.md specification, in order. For each section:
1. State “PASS” if your executed plan or code implementation(s) or action(s) fully satisfies that section, or “FAIL” if not.
2. Give a one-paragraph explanation of how it’s met (or what’s missing).
Use the exact section headers. Proceed until every section is covered.