{
  "name": "ventry",
  "version": "0.0.0",
  "private": true,
  "description": "AI-Native Inventory Management System",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "test:unit": "turbo test --filter='!@ventry/e2e'",
    "test:unit:fast": "turbo test:fast --filter='!@ventry/e2e'",
    "test:integration": "turbo test:integration",
    "test:e2e": "turbo test:e2e",
    "test:e2e:ui": "turbo test:ui --filter='@ventry/e2e'",
    "test:e2e:debug": "turbo test:debug --filter='@ventry/e2e'",
    "test:cov": "turbo test:cov",
    "db:generate": "turbo db:generate",
    "db:push": "turbo db:push",
    "db:migrate": "turbo db:migrate",
    "db:migrate:deploy": "turbo db:migrate:deploy",
    "db:seed": "turbo db:seed",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md,mdx}\"",
    "clean": "turbo clean && rm -rf node_modules",
    "prepare": "husky install",
    "playwright:install": "playwright install --with-deps",
    "ci:setup": "./tools/scripts/setup-github-repo.sh && ./tools/scripts/setup-ci-secrets.sh",
    "ci:validate": "./tools/scripts/validate-ci-setup.sh"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@sentry/nextjs": "^9.35.0",
    "@sentry/node": "^7.91.0",
    "@types/node": "^20.10.0",
    "eslint": "^8.54.0",
    "husky": "^8.0.3",
    "lint-staged": "^15.1.0",
    "playwright": "^1.40.0",
    "prettier": "^3.1.0",
    "prisma": "^6.11.1",
    "turbo": "^1.13.4",
    "typescript": "^5.3.0"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md,mdx,yml,yaml}": [
      "prettier --write"
    ]
  },
  "packageManager": "pnpm@8.10.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  }
}
